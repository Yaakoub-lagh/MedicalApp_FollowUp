import React from 'react';
import { Typography } from '@mui/material';

function Parametre() {
  return (
    <Typography variant="h4" color="textPrimary">
      Update your preferences and settings in the Parametre Tab.
    </Typography>
  );
}

export default Parametre;
